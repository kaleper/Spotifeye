function test () {
    for (each t in tracks) {
                let artist_name = t.track.artists[0].name
                let track_name = t.track.name
                let track_preview = t.track.preview_url
              
}
}

// <!-- <td> artist_name
//     <td> track_name
//     <td> a(href=track_preview) Preview </td> -->